<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>net.sourceforge.czt</groupId>
    <artifactId>corejava</artifactId>
    <version>1.6-SNAPSHOT</version>
  </parent>

  <artifactId>corejava-circuspatt</artifactId>
  <packaging>jar</packaging>

  <name>CZT Corejava Circus Pattern</name>
  <description>
    Provides AST classes for Circus Pattern extension.
  </description>
  
  <dependencies>
    <dependency>
      <groupId>${czt.groupId}</groupId>
      <artifactId>corejava-circus</artifactId>
    </dependency>
    <dependency>
      <groupId>${czt.groupId}</groupId>
      <artifactId>corejava-z</artifactId>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>

      <plugin>
        <groupId>${czt.groupId}</groupId>
        <artifactId>gnast-maven-plugin</artifactId>
        <executions>
          <execution>
            <configuration>
              <targetNamespace>http://czt.sourceforge.net/circuspatt</targetNamespace>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>xml-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>generate-chars</id>
            <phase>generate-sources</phase>
            <configuration>
              <transformationSets>
                <transformationSet>
                  <dir>${xml.dir}</dir>
                  <includes>
                    <include>circuspattchar.xml</include>
                  </includes>
                  <outputDir>${build.xml.dir}/</outputDir>
                    <fileMappers>
                      <fileMapper implementation="org.codehaus.plexus.components.io.filemappers.MergeFileMapper">
                        <targetName>net/sourceforge/czt/circuspatt/util/CircusPattChar.java</targetName>
                      </fileMapper>
                    </fileMappers>
                  <stylesheet>${xsl.dir}/circuspattchar2class.xsl</stylesheet>
                </transformationSet>
                <transformationSet>
                  <dir>${xml.dir}</dir>
                  <includes>
                    <include>circuspattchar.xml</include>
                  </includes>
                  <outputDir>${build.xml.dir}/</outputDir>
                    <fileMappers>
                      <fileMapper implementation="org.codehaus.plexus.components.io.filemappers.MergeFileMapper">
                        <targetName>net/sourceforge/czt/circuspatt/util/CircusPattString.java</targetName>
                      </fileMapper>
                    </fileMappers>
                  <stylesheet>${xsl.dir}/circuspattchar2stringclass.xsl</stylesheet>
                </transformationSet>
              </transformationSets>
            </configuration>
	        <goals>
	          <goal>transform</goal>
	        </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
      </plugin>

      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
      </plugin>

    </plugins>
  </build>
</project>

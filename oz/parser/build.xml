<project name="ozparser" default="compile" basedir=".">
  <property name="czt.home" location="../.."/>
  <property file="${czt.home}/czt.properties"/>
  <property file="build.properties"/>
  <property name="src" location="${src.dir}"/>
  <property name="czt.lib" location="${czt.home}/lib"/>
  <property name="jar.file" location="${czt.lib}/${oz.parser.jar.file}"/>

  <path id="classpath">
    <pathelement path="${jwsdp.classpath}"/>
    <pathelement path="${build.dir}"/>
    <pathelement path="${javacup.home}"/>
    <fileset dir="${czt.lib}">
      <include name="*.jar"/>
    </fileset>
    <pathelement location="."/>
    <pathelement location="${czt.lib}/${parser.jar.file}"/>
  </path>

  <target name="install" depends="compile" description="build the jar file">
    <jar jarfile="${jar.file}">
      <fileset dir="${build.dir}"/>
    </jar>
  </target>

  <target name="compile" depends="builddir,scanner,uscanner,parser" description="Make the parser">
    <javac 
      srcdir="${src}"
      destdir="${build.dir}"
      debug="true">
      <classpath refid="classpath"/>
    </javac>
  </target>

  <target name="code" depends="builddir" description="Build the code only">
    <javac 
      srcdir="${src}"
      destdir="${build.dir}"
      debug="true">
      <classpath refid="classpath"/>
    </javac>
  </target>

  <!-- This will not work with versions of JavaCup < 1.0k. Run the shell
       script cup.sh to build the parser and scanner instead.
       Then run 'ant code'.-->
  <target name="parser">
    <taskdef classname="ant.JavaCupTask" name="java_cup"
        classpath="${javacuptask.home}"/>
    <java_cup
      srcDir="${src}/${package.dir}/"
      destDir="${src}/${package.dir}/"
      inputFile="${parser.name}.cup"
      parserFile="${parser.name}"
      symbolFile="${sym.name}"
    />
  </target>

  <!-- The older version of this task is as follows. If you do not have
       the JavaCup task, this can be used. This will also not work with
       versions of JavaCup < 1.0k
  <target name="parser" description="Create the parser">
    <java classname="java_cup.Main" fork="true"
          dir="${src}/${package.dir}/">
      <arg line="-parser ${parser.name} 
           -symbols ${sym.name} ${parser.name}.cup"/>
      <classpath refid="classpath"/>
    </java>
  </target>-->

  <taskdef classname="JFlex.anttask.JFlexTask" name="jflex" />

  <target name="scanner" description="Create the scanner">
    <jflex file="${src}/${package.dir}/${scanner.name}.flex"
      destdir="${src}/"
      skipMinimization="yes"
    />
  </target>

  <target name="uscanner" description="Create the unicode scanner">
    <jflex file="${src}/${package.dir}/UnicodeScanner.jflex"
      destdir="${src}/"
      skipMinimization="yes"
    />
  </target>

  <target name="printclasspath">
      <pathconvert targetos="unix" property="tmp.clspath"
            refid="classpath">
    </pathconvert>
    <echo message="CLASSPATH = ${tmp.clspath}"/>
  </target>

  <target name="builddir" description="Make the build directory">
    <mkdir dir="${build.dir}"/>
  </target>

  <target name="checkstyle" description="check java sources">
    <taskdef resource="checkstyletask.properties"
      classpath="${checkstyle.jar}"/>
    <checkstyle config="../../devtools/checkstyle.xml">
      <fileset 
        dir="${src}/${package.dir}"
        excludes="${scanner.name}.java,
                  ${parser.name}.java,
                  ${sym.name}.java"/>
      <formatter type="plain"/>
    </checkstyle>
  </target>

  <target name="run" depends="compile" description="Run the GUI">
    <java classname="${package.name}.Main" fork="true">
      <classpath refid="classpath"/>
    </java>
  </target>

  <target name="run2" description="Run the GUI without building">
    <java classname="${package.name}.Main" fork="true">
      <classpath refid="classpath"/>
    </java>
  </target>

  <target name="tests" depends="code" description="Run OpTable tests">
    <java classname="${package.name}.OperatorTableTest" fork="true">
      <classpath refid="classpath"/>
    </java>
  </target>

  <target name="clean"
          description="clean up">
    <delete dir="${build.dir}"/>
    <delete file="${src.dir}/${package.dir}/${scanner.name}.java"/>
    <delete file="${src.dir}/${package.dir}/UnicodeScanner.java"/>
    <delete file="${src.dir}/${package.dir}/${parser.name}.java"/>
    <delete file="${src.dir}/${package.dir}/${sym.name}.java"/>
  </target>
</project>

\begin{zsection}
   \SECTION AxParaTest \parents standard\_toolkit
\end{zsection}

\begin{schema}{S}
   x, y: \nat
\where
   x > y \\
   x \in \num
\end{schema}

\begin{zed}
   [GP]
\end{zed}

\begin{axdef}
   a, b: \power~(\nat \cross GP)
\where
   \lnot a = \{\} \\
   a = b
\end{axdef}

% No support for application expression yet.
% a, b: \seq~(\nat \cross GP)
% a \cat b = b \cat a \\
% a \neq \langle \rangle

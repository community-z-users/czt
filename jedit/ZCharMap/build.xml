<?xml version="1.0"?>
<project name="charactermap" default="install" basedir=".">
  <property name="czt.home" location="../.."/>
  <property file="${czt.home}/czt.properties"/>
  <property name="czt.jar" location="${czt.home}/${jar.dir}/${czt.jar.file}"/>
  <property name="jar.name"  value="${plugin.jar.file}"/>
  <property name="src.dir" value="."/>
  <property name="build.dir" value="build"/>
  <property name="install.dir" value="${czt.home}/${jar.dir}"/>
  
  <path id="project.class.path">
    <fileset dir="${czt.home}/${jar.dir}">
      <include name="${corejava.base.jar.file}"/>
      <include name="${corejava.core.jar.file}"/>
      <include name="${corejava.oz.jar.file}"/>
      <include name="${corejava.jaxb.jar.file}"/>
      <include name="${java_cup.jar.file}"/>
      <include name="${session.jar.file}"/>
      <include name="${parser.jar.file}"/>
      <include name="${typechecker.jar.file}"/>
    </fileset>
    <pathelement path="${jedit.jar}"/>
    <pathelement path="${jedit-errorlist.jar}"/>
    <pathelement path="${jwsdp.classpath}"/>
    <pathelement path="${java.class.path}"/>
    <pathelement path="${ant.home}/lib/clover.jar"/>
  </path>
  
  
  <target name="init">
    <mkdir dir="${build.dir}"/>
  </target>
  
  
  <target name="compile" depends="init">
    <javac
      srcdir="${src.dir}"
      destdir="${build.dir}"
      deprecation="on"
      includeJavaRuntime="yes"
      debug="yes"
      >
      <classpath refid="project.class.path"/>
    </javac>
  </target>
  
  <target name="install" depends="compile">
    <mkdir dir="${install.dir}"/>
    <jar jarfile="${install.dir}/${jar.name}">
      <fileset dir="${build.dir}"/>
      <fileset dir="${src.dir}">
        <include name="actions.xml"/>
        <include name="dockables.xml"/>
        <include name="**/*.props"/>
        <include name="**/*.html"/>
        <include name="**/*.xml"/>
      </fileset>
    </jar>
  </target>

  <target name="clean">
    <delete dir="${build.dir}"/>
    <delete>
      <fileset dir="." includes="**/*~" defaultexcludes="no"/>
    </delete>
  </target>
</project>

<project name="zlive-jedit-plugin" default="all">
  <property name="czt.home" location="../.."/>
  <property file="${czt.home}/czt.properties"/>
  <property name="zlive-jedit-plugin.jar.dir"
    location="${czt.home}/${jar.dir}"/>
  <property name="jar.file"
    location="${zlive-jedit-plugin.jar.dir}/${zlive-jedit-plugin.jar.file}"/>
  <property name="zlive-jedit-plugin.src.dir" location="src/main/java"/>
  <property name="zlive-jedit-plugin.build.dir" location="build"/>
  <property name="zlive-jedit-plugin.api.dir" location="doc/api"/>

  <path id="zlive-jedit-plugin.class.path">
    <fileset dir="${czt.home}/${jar.dir}">
      <include name="${corejava.zpatt.jar.file}"/>
      <include name="${zlive.jar.file}"/>
    </fileset>
    <pathelement path="${jedit.jar}"/>
    <pathelement path="${jedit-console.jar}"/>
    <pathelement path="${jedit-errorlist.jar}"/>
    <pathelement path="${java.class.path}"/>
    <pathelement path="${ant.home}/lib/clover.jar"/>
  </path>

  <target name="init">
    <tstamp/>
    <mkdir dir="${zlive-jedit-plugin.build.dir}/classes"/>
  </target>

  <target name="all" depends="install, doc"
    description="build the jar files and the documentation"/>

  <target name="install" depends="compile"
          description="build and install the jar files">
    <mkdir dir="${zlive-jedit-plugin.jar.dir}"/>
    <mkdir dir="${zlive-jedit-plugin.build.dir}/external"/>
    <unjar src="${czt.home}/${jar.dir}/${corejava.zpatt.jar.file}"
      dest="${zlive-jedit-plugin.build.dir}/external"/>
    <unjar src="${czt.home}/${jar.dir}/${zlive.jar.file}"
      dest="${zlive-jedit-plugin.build.dir}/external"/>
    <jar jarfile="${jar.file}">
      <fileset dir="${zlive-jedit-plugin.build.dir}/classes"/>
      <fileset dir="src/main/resources"/>
      <fileset dir="${zlive-jedit-plugin.build.dir}/external"/>
    </jar>
  </target>

  <target name="compile" depends="init"
          description="compile the sources">
    <javac
      srcdir="${zlive-jedit-plugin.src.dir}"
      destdir="${zlive-jedit-plugin.build.dir}/classes"
      debug="${debug}"
      debuglevel="${debuglevel}"
      optimize="${optimize}">
      <classpath refid="zlive-jedit-plugin.class.path"/>
      <compilerarg value="${compilerarg}"/>
    </javac>
  </target>

  <target name="doc" depends="api"
    description="build all the documentation"/>

  <target name="api"
          description="build the API documentation">
    <mkdir dir="${zlive-jedit-plugin.api.dir}"/>
    <javadoc
      destdir="${zlive-jedit-plugin.api.dir}"
      private="true">
      <packageset dir="${zlive-jedit-plugin.src.dir}"/>
      <classpath refid="zlive-jedit-plugin.class.path"/>
      <tag name="czt.todo" description="To do:"/>
    </javadoc>
  </target>

  <target name="checkstyle" description="check java sources">
    <taskdef resource="checkstyletask.properties"
      classpath="${checkstyle.jar}"/>
    <checkstyle config="${czt.home}/devtools/checkstyle.xml">
      <fileset dir="${zlive-jedit-plugin.src.dir}"/>
      <formatter type="plain"/>
    </checkstyle>
  </target>

  <target name="clean"
          description="clean up">
    <delete dir="${zlive-jedit-plugin.build.dir}"/>
    <delete dir="${zlive-jedit-plugin.api.dir}"/>
  </target>
</project>

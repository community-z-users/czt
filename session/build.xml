<project name="session" default="all">
  <property name="czt.home" location=".."/>
  <property file="${czt.home}/czt.properties"/>
  <property name="session.jar.dir" location="${czt.home}/${jar.dir}"/>
  <property name="jar.file" location="${session.jar.dir}/${session.jar.file}"/>
  <property name="session.src.dir" location="src/main/java"/>
  <property name="session.build.dir" location="build"/>
  <property name="session.api.dir" location="doc/api"/>
  <property name="test.src.dir" location="src/test/java"/>
  <property name="test.build.dir" location="${session.build.dir}/test"/>
  <property name="test.jar.file" location="${session.build.dir}/tests.jar"/>

  <target name="init">
    <tstamp/>
    <mkdir dir="${session.build.dir}"/>
    <mkdir dir="${test.build.dir}"/>
  </target>

  <target name="all" depends="install, doc"
    description="build the jar files and the documentation"/>

  <target name="install" depends="compile"
          description="build and install the jar files">
    <mkdir dir="${session.jar.dir}"/>
    <jar jarfile="${jar.file}">
      <fileset dir="${session.build.dir}"/>
      <fileset dir="src/main/resources"/>
    </jar>
  </target>

  <target name="compile" depends="init"
          description="compile the sources">
    <javac
      srcdir="${session.src.dir}"
      destdir="${session.build.dir}"
      debug="${debug}"
      debuglevel="${debuglevel}"
      optimize="${optimize}">
      <!-- <compilerarg value="${compilerarg}"/> -->
    </javac>
  </target>

  <target name="test" depends="run-tests">
    <junitreport todir="${session.build.dir}">
      <fileset dir="${session.build.dir}">
        <include name="TEST-*.xml"/>
      </fileset>
      <report format="frames" todir="${session.build.dir}/report"/>
    </junitreport>
  </target>

  <target name="compile-tests" depends="install">
    <javac
      srcdir="${test.src.dir}"
      destdir="${test.build.dir}"
      debug="${debug}"
      debuglevel="${debuglevel}"
      optimize="${optimize}">
      <classpath>
        <pathelement location="${jar.file}"/>
      </classpath>
      <compilerarg value="-Xlint"/>
    </javac>
  </target>

  <target name="install-tests" depends="compile-tests">
    <mkdir dir="${session.build.dir}"/>
    <jar jarfile="${test.jar.file}">
      <fileset dir="${test.build.dir}"/>
    </jar>
  </target>

  <target name="run-tests" depends="install-tests">
    <junit fork="yes" printsummary="on">
      <formatter type="xml"/>
      <test toDir="${session.build.dir}"
        name="net.sourceforge.czt.session.SectionManagerTest"/>
      <classpath>
        <pathelement location="${jar.file}"/>
        <pathelement location="${test.jar.file}"/>
      </classpath>
    </junit>
  </target>

  <target name="doc" depends="api"
    description="build all the documentation"/>

  <target name="api"
          description="build the API documentation">
    <mkdir dir="${session.api.dir}"/>
    <javadoc
      destdir="${session.api.dir}"
      private="true">
      <packageset dir="${session.src.dir}"/>
      <tag name="czt.todo" description="To do:"/>
    </javadoc>
  </target>

  <target name="checkstyle" description="check java sources">
    <taskdef resource="checkstyletask.properties"
      classpath="${checkstyle.jar}"/>
    <checkstyle config="${czt.home}/devtools/checkstyle.xml">
      <fileset dir="${session.src.dir}"/>
      <formatter type="plain"/>
    </checkstyle>
  </target>

  <target name="clean"
          description="clean up">
    <delete dir="${session.build.dir}"/>
    <delete dir="${session.api.dir}"/>
  </target>
</project>
